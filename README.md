# QtSpellcheckingWindowsTSF

A simple, but not battle-tested use of the Windows Spellcheck API in a QML project.
Because TextArea inside a QML project doesn't have a spellcheck out-of-the-box.

Windows only.